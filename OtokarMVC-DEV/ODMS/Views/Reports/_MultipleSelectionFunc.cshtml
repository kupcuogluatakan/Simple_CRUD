
<script>
    function GetQuotedList(list) {
        if (list != '') {
            list = '\'' + list + '\'';

            list = list.replace(new RegExp(',', 'g'), "','");

        }
        return list;
    }
</script>
